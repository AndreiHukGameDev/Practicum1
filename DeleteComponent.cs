using System;
using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class DeleteComponent : MonoBehaviour
{
    // Start is called before the first frame update
    public GameObject objectFrom;

    // Update is called once per frame
    void FixedUpdate()
    {
        
        //if (objectFrom.GetComponent<T>())
        //{
        //    DestroyImmediate(objectFrom.GetComponent<T>(), true);
        //    Debug.Log($"Component {T} was deleted!");
        //}
        //else
        //{
        //    Debug.Log("Finish Deleted");
        //}
    }
}
